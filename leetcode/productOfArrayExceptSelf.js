// an integer arr nums, return an answer such that answer[i] is equal to product of all elements of nums except nums[i]

//[1,2,3,4] ----> [24,12,8,6]

const productOfNumsExceptSelf = (input=[]) => {
    if (input.length === 0) return []
    
    for (let i = 0; i < input.length; i++){
        
    }
}